# 复制为 .env 后填写（.env 已被 .gitignore 忽略，不会入库）：

# JWT 签名密钥（HS256，后端无默认值，缺失直接启动失败）。生成方式见 README.md
JWT_SECRET=

# 数据库密码（可选，默认为空）。容器内 MySQL 的 root 默认无密码。
# 只有当你自己在库里执行了 ALTER USER 设密码之后，才需要把同一值填到这里。
# 填写后需 docker compose up -d 重建容器使其生效。
# MYSQL_PASSWORD=
